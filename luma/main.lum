class parent {
    constructor test (p1, p2){
        set this.hh to p1
        set this.hi to p2
    }

    function method (param){
        show ("hi " + param + " " + this.hi)
    }
}

class child (parent) {
    function reef (name){
        return "reef! " + name + "!"
    }
}


set instance to child ("hi", "reef")
show (instance.method ("hello"))

function hi (){
    set tmp to 0
    show (tmp)
}

hi ()
show (tmp)
